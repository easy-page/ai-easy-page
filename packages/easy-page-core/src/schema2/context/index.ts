import { StoreContext } from './store';

export const context = {
	store: StoreContext,
};

export { store } from './store';
export { values } from './values';
export { changedValues } from './changedValues';
export { allValues } from './allValues';
